export const callbackStatus = ({ status, message }) => {
    return {
        status,
        message
    };
};

export const callbackData = ({ status, data }) => {
    return {
        status,
        data
    };
};
